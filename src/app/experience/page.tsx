import { ArrowUpRightIcon } from 'lucide-react'
import { Position, PositionList } from './position'
import { redirect } from 'next/navigation'

export default async function HotsaucePage() {
  redirect('experience/hotsauce')
}
